{
  "name": "landing-fullstack",
  "private": true,
  "workspaces": ["client"],
  "scripts": {
    "dev": "npm run dev --workspace client",
    "build": "npm run build --workspace client"
  },
  "devDependencies": {}
}
